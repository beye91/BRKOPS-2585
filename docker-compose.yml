# =============================================================================
# BRKOPS-2585: AI-Driven Network Operations Demo Platform
# 4-Container Architecture: Frontend, Backend, PostgreSQL, Redis
# =============================================================================

services:
  # ===========================================================================
  # FRONTEND - Next.js 14 Application
  # ===========================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # These are BUILD-TIME variables - must be external URLs for browser access
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://198.18.134.22:8003}
        - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL:-ws://198.18.134.22:8003}
    container_name: brkops-frontend
    restart: unless-stopped
    ports:
      - "3003:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://198.18.134.22:8003}
      - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL:-ws://198.18.134.22:8003}
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - brkops-network
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://0.0.0.0:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ===========================================================================
  # BACKEND - FastAPI + arq Worker (Single Container)
  # ===========================================================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: brkops-backend
    restart: unless-stopped
    ports:
      - "8003:8000"
    environment:
      # Database
      - POSTGRES_HOST=${POSTGRES_HOST:-postgres}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - POSTGRES_DB=${POSTGRES_DB:-brkops2585}
      - POSTGRES_USER=${POSTGRES_USER:-brkops}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      # Redis
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      # Server
      - BACKEND_HOST=${BACKEND_HOST:-0.0.0.0}
      - BACKEND_PORT=${BACKEND_PORT:-8000}
      - SECRET_KEY=${SECRET_KEY:-changeme}
      - DEBUG=${DEBUG:-false}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      # LLM Providers
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4-turbo-preview}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-3-sonnet-20240229}
      - LLM_TEMPERATURE=${LLM_TEMPERATURE:-0.7}
      - LLM_MAX_TOKENS=${LLM_MAX_TOKENS:-4096}
      - LLM_TIMEOUT=${LLM_TIMEOUT:-30}
      # MCP Servers
      - CML_MCP_URL=${CML_MCP_URL:-}
      - CML_HOST=${CML_HOST:-}
      - CML_USERNAME=${CML_USERNAME:-}
      - CML_PASSWORD=${CML_PASSWORD:-}
      - SPLUNK_MCP_URL=${SPLUNK_MCP_URL:-}
      - SPLUNK_HOST=${SPLUNK_HOST:-}
      - SPLUNK_TOKEN=${SPLUNK_TOKEN:-}
      # Notifications
      - WEBEX_WEBHOOK_URL=${WEBEX_WEBHOOK_URL:-}
      - WEBEX_BOT_TOKEN=${WEBEX_BOT_TOKEN:-}
      - WEBEX_ROOM_ID=${WEBEX_ROOM_ID:-}
      - SERVICENOW_INSTANCE=${SERVICENOW_INSTANCE:-}
      - SERVICENOW_USERNAME=${SERVICENOW_USERNAME:-}
      - SERVICENOW_PASSWORD=${SERVICENOW_PASSWORD:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - backend-uploads:/app/uploads
    networks:
      - brkops-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ===========================================================================
  # POSTGRESQL - Database
  # ===========================================================================
  postgres:
    image: postgres:16-alpine
    container_name: brkops-postgres
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-brkops2585}
      - POSTGRES_USER=${POSTGRES_USER:-brkops}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
      - ./scripts/seed-data.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    networks:
      - brkops-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-brkops} -d ${POSTGRES_DB:-brkops2585}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ===========================================================================
  # REDIS - Job Queue + Pub/Sub
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: brkops-redis
    restart: unless-stopped
    ports:
      - "6383:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - brkops-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

# =============================================================================
# NETWORKS
# =============================================================================
networks:
  brkops-network:
    driver: bridge
    name: brkops-network

# =============================================================================
# VOLUMES
# =============================================================================
volumes:
  postgres-data:
    name: brkops-postgres-data
  redis-data:
    name: brkops-redis-data
  backend-uploads:
    name: brkops-backend-uploads
